# Review TODOs

- Resolve audit metadata field naming/shape in RequestMeta (e.g., audit_reference vs audit_context). Current implementation exposes only `audit_requested`; confirm whether additional fields should be added and documented.
