# TC-B2-E02

Version-specific examples.

Simulate:
- Python 3.9 branch uses `typing.Optional`
- Python 3.12 branch uses `|` union syntax
- optional dependency path for pandas extra

Expected HuTouch behavior:
- preserve both branches
